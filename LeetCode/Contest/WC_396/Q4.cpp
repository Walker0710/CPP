#include <bits/stdc++.h>

using namespace std;

// << ,

int minCostToEqualizeArray(vector<int> &nums, int cost1, int cost2)
{
    int n = nums.size();
    sort(nums.begin(), nums.end());
    int maxi = nums[n-1];

    if(2*cost1 <= cost2) {

    }  

    else {
        
    }
}