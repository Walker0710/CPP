#include <bits/stdc++.h>

using namespace std;

// << ,

int minimumNumberOfSwaps(string& s)
{
    for(int i = 0; i < s.size(); i++)
    {
        if(s[i] == '[' && )
    }

    return (maxClose + 1) / 2;
}
