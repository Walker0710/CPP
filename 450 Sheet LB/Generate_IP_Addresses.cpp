#include <bits/stdc++.h>

using namespace std;

// << ,

void solve(int ind, string &s, string curr, vector<string> &ans) {

    string temp = "";
    temp = temp + s[ind]

    

}

vector<string> generateIp(string s)
{
    vector<string> ans;
    
    solve(0, s, "", ans);

    return ans;
}   