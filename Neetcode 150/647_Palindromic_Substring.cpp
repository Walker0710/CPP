#include <bits/stdc++.h>

using namespace std;

// << ,

int solve(string &s, int i, int j) {
    if(i > j) {
        return 0;
    }

    if()
}

int countSubstrings(string s)
{
    return solve(s, 0, s.length() - 1);
}