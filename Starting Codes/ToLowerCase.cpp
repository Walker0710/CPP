#include<iostream>

using namespace std;

char ToLowerCase(char ch) {

    if(char >= 'a' && char <= 'z') {

        return ch;
    }

    else{

        char temp = ch - 'A' + 'a';
    }
}
